import * as chai from 'chai';

describe("Calculator", () => {
    describe("add", () => {
        it("should return 3 when adding 1 and 2", () => {
            chai.assert.isTrue(true);
        });
    });
});